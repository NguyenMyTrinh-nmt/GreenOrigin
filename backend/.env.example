# Environment Variables
NODE_ENV=development
PORT=5000

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/greenorigin
# Hoặc sử dụng MongoDB Atlas:
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/greenorigin?retryWrites=true&w=majority

# JWT Secret Key (Thay đổi thành chuỗi bí mật của bạn)
JWT_SECRET=your_very_secure_jwt_secret_key_here_change_this_in_production
JWT_EXPIRE=7d

# Blockchain Configuration (Ethereum/Polygon network)
# Sepolia Testnet RPC
BLOCKCHAIN_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_PROJECT_ID
# Hoặc Polygon Mumbai Testnet:
# BLOCKCHAIN_RPC_URL=https://rpc-mumbai.maticvigil.com

# Smart Contract Address (sau khi deploy smart contract)
CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000

# Private Key cho server wallet (chỉ dùng cho testnet, không public lên git)
SERVER_PRIVATE_KEY=your_private_key_here

# CORS Origin
CORS_ORIGIN=http://localhost:3000
